package taller

class Ejercicio() {

  // Recorre n términos: el primero es 1 y cada uno sale del anterior
  // aplicando g. Cada término se eleva a la p y se combina con f.
  // Tal como está devuelve siempre 0 y las pruebas quedan en rojo.
  def opCurrified(n: Int)(p: Int)(f: (Int, Int) => Int)(g: Int => Int): Int = {
    0 // Completar
  }
}
